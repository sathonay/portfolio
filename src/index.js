import React from 'react';
import ReactDOM from 'react-dom/client';
import Main from './ui/page/Main.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Main />
  </React.StrictMode>
);